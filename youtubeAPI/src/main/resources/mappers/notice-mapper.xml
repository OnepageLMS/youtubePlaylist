<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="Notice">
	<insert id="insertNotice" useGeneratedKeys="true" keyProperty="id">
		insert into lms_notice (classID, title, content, important)
		values (#{classID}, #{title}, #{content}, #{important})
	</insert>
	
	<update id="updateNotice">
		update lms_notice 
		set title=#{title}, content=#{content}, important=#{important}
		where id=#{id}
	</update>
	
	<delete id="deleteNotice">
		delete from lms_notice where id=#{id}
	</delete>
	
	<select id="getAllNotice" resultType="notice">
		select * from lms_notice
		where classID=#{classID} order by id desc
	</select>
	
	<insert id="insertNoticeCheckRecord">
		insert into lms_noticeCheck (noticeID, studentID)
		values (#{id}, #{studentID})
	</insert>
</mapper>